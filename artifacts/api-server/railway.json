{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm i -g pnpm && pnpm install --ignore-scripts"
  },
  "deploy": {
    "startCommand": "node dist/index.mjs",
    "restartPolicyType": "ON_FAILURE"
  }
}